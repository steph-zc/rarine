package com.rarine.repository;

import com.rarine.domain.entity.EmbroideryColor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EmbroideryColorRepository extends JpaRepository<EmbroideryColor, Long> {}
